// Code generated by scripts/build-api-client.mjs. DO NOT EDIT.
// Source of truth: src/pages/api-docs/endpoints.ts
//
// One typed function per backend endpoint, grouped by domain. Path params are
// positional args; query/body params are a single typed object. Every call
// returns the standard `Msg<T>` envelope. Response payloads are `unknown` by
// design — validate them at the call site with the matching zod schema in
// src/schemas/. Pass a type argument to narrow: `clientsApi.list<ClientRecord[]>()`.
/* eslint-disable @typescript-eslint/no-explicit-any */
import { HttpUtil, type Msg, type HttpOptions } from '@/utils';

const JSON_HEADERS: HttpOptions = { headers: { 'Content-Type': 'application/json' } };

function enc(v: string | number): string {
  return encodeURIComponent(String(v));
}

/** Client */
export const clientApi = {
  /** Everything the shell needs in one call: whether a subscription has been imported at all, whether the tunnel is up, whether the imported key is an admin one, and the current toggle positions. Drives the choice between the import screen and t */
  clientApiState<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/client/api/state', params, options);
  },

  /** Import a connection URI. The client stores it, contacts a node to find out what the key grants, and returns the resulting state — including whether this key makes the holder an admin. That answer comes from the node, never from parsing the  */
  clientApiImport<T = unknown>(body: { uri: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/import', body, { ...JSON_HEADERS, ...options });
  },

  /** Bring the tunnel up. The client races every entrypoint it knows and keeps whichever answers first; the losers are told to stop expecting it. Returns once a winner is chosen, not once the first packet flows. */
  clientApiConnect<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/connect', body, options);
  },

  /** Take the tunnel down and release the adapter. Routing rules and toggles are remembered. */
  clientApiDisconnect<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/disconnect', body, options);
  },

  /** Flip the exit and adblock switches. Adblock takes effect immediately — it is only a list in the local resolver. Exit applies to NEW flows: an established connection cannot be moved to a different exit without the far end seeing its peer cha */
  clientApiToggle<T = unknown>(body: { egress?: boolean; adblock?: boolean }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/toggle', body, { ...JSON_HEADERS, ...options });
  },

  /** Fetch the subscription now instead of waiting for the interval. Works with or without the tunnel up — it rides the same transport but does not need it. If the node list changed but the node currently carrying the tunnel is still in it, the  */
  clientApiSubscriptionRefresh<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/subscription/refresh', body, options);
  },

  /** The entrypoints this subscription grants, with the measured latency of the last race and which one currently carries the tunnel. An admin sees every node in the network rather than the group's. */
  clientApiNodes<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/client/api/nodes', params, options);
  },

  /** What the daemon has to say, newest first. The log lives in the daemon rather than in the page because the tunnel keeps running with no browser open: an automatic subscription refresh, a re-race after the network changed, a refused exit — al */
  clientApiNotifications<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/client/api/notifications', params, options);
  },

  /** Mark one notification read, or all of them with id 0. Closing the bell is what sends this, not opening it: acknowledging on open would wipe the "new" highlight before it has been read, which is the one thing the highlight exists for. Read e */
  clientApiNotificationsRead<T = unknown>(body: { id: number }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/notifications/read', body, { ...JSON_HEADERS, ...options });
  },

  /** Drop one notification for good. This is what the cross on a row sends: a row the reader has dealt with should leave the strip rather than sit there marked read, which is the difference between an acknowledgement and a filed message. Unlike  */
  clientApiNotificationsDismiss<T = unknown>(body: { id: number }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/notifications/dismiss', body, { ...JSON_HEADERS, ...options });
  },

  /** Throw the log away. Deliberate and unrecoverable — nothing here is stored anywhere else. */
  clientApiNotificationsClear<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/notifications/clear', body, options);
  },

  /** The counters the tunnel already writes to its console, kept in a ring on this machine and never sent anywhere. Every series comes back in one call — the whole window is a few hundred numbers, and separate requests per metric would only risk */
  clientApiHistoryByWindow<T = unknown>(window: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/client/api/history/${enc(window)}`, params, options);
  },

  /** The per-process routing rules and the default everything without a rule falls back to. Processes only — no domains, no regex, no geoip: attribution comes from the connection table, which knows a local port and a PID and nothing else. `apply */
  clientApiRouting<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/client/api/routing', params, options);
  },

  /** Replace the whole rule set and the default in one write. A full replace rather than per-rule edits: the set is a handful of rows, and replacing it cannot half-apply or leave two rules fighting over one process. Ids are assigned here, not se */
  postClientApiRouting<T = unknown>(body: { defaultRole: string; rules: unknown[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/routing', body, { ...JSON_HEADERS, ...options });
  },

  /** What is running with an open connection right now, for the rule picker. Read out of the same connection table the datapath uses for attribution, so it costs nothing extra. `connections` is how many flows the process holds — the page sorts b */
  clientApiRoutingProcesses<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/client/api/routing/processes', params, options);
  },

  /** Everything the client remembers about how it should behave: how often to refresh the subscription, and what to do at startup. Appearance is stored here too — the page is served by this process, so the look belongs to the client and not to t */
  clientApiSettings<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/client/api/settings', params, options);
  },

  /** Persist the client settings. The page edits a draft and sends the whole set on Save, so a half-typed number never reaches the daemon; every field is optional and an absent one is left as it was. None of this needs the tunnel to come down. T */
  postClientApiSettings<T = unknown>(body: { refreshMinutes: number; autostart: boolean; autostartBehaviour: string; manualBehaviour: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/settings', body, { ...JSON_HEADERS, ...options });
  },

  /** What this client is, as the network sees it: when the record was created, how much it has moved, when the subscription runs out and where the traffic went. The site list is read out of the local resolver and never leaves the machine — it is */
  clientApiAbout<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/client/api/about', params, options);
  },

  /** Forget local state — preferences, routing rules and the site list the local resolver keeps. With subscription=true the imported link goes too and the next start opens the import screen again. The tunnel is not torn down by the first form; t */
  clientApiReset<T = unknown>(body: { subscription?: boolean }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/client/api/reset', body, { ...JSON_HEADERS, ...options });
  },
};

/** Entrypoints */
export const inboundsApi = {
  /** Every entrypoint with its node, port and traffic counters. The counters are telemetry collected from the nodes, not something the panel maintains. */
  list<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/inbounds/list', params, options);
  },

  /** Picker projection: id, node, tag, port and the enable flag, without counters or certificate state. Feeds the entrypoint chooser in the group editor. */
  options<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/inbounds/options', params, options);
  },

  /** One entrypoint by id. */
  getById<T = unknown>(id: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/inbounds/get/${enc(id)}`, params, options);
  },

  /** Open a new entrypoint on a node. The port must be free on that node — the check is per node, not global, since two nodes may both listen on 443. Draft edit. */
  add<T = unknown>(body: { nodeId: number; port: number; remark: string; enable: boolean }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/inbounds/add', body, { ...JSON_HEADERS, ...options });
  },

  /** Change an entrypoint: port, name and enable flag. Moving the port rewrites the connection URI of every client whose group holds this entrypoint — they need the new link once the draft is published. */
  updateById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/update/${enc(id)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Flip only the enable flag. A disabled entrypoint stops accepting connections but stays in its groups, so re-enabling it does not require touching any client. */
  setEnableById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/setEnable/${enc(id)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Close an entrypoint and drop it from every group holding it. Clients whose group is left with no entrypoints keep their records but lose their route — check the group before deleting. Draft edit. */
  delById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/del/${enc(id)}`, body, options);
  },

  /** Zero the accumulated traffic of one entrypoint. Panel-side baseline shift, like the client counterpart — the node keeps counting and is not told. */
  resetTrafficById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/${enc(id)}/resetTraffic`, body, options);
  },

  /** Zero the accumulated traffic of every entrypoint. Collected history is discarded and cannot be recovered from the nodes — their own counters restarted at their last datapath start, not at yours. */
  resetAllTraffics<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/inbounds/resetAllTraffics', body, options);
  },
};

/** Server */
export const serverApi = {
  /** Snapshot of the machine the panel runs on — CPU, memory, swap, disk, network IO, load, open sockets. This is the operator's own workstation, not a node: node health comes from /nodes/list and /nodes/{id}/history. Cached briefly and refreshe */
  status<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/status', params, options);
  },
};

/** Clients */
export const clientsApi = {
  /** Every client in the draft, unpaginated. Used where the whole set has to be shown at once — picking group members, for instance. The clients page uses /list/paged instead. */
  list<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/clients/list', params, options);
  },

  /** Filter, sort and paginate clients server-side. Search matches the tag and the comment only. Sortable columns: createdAt, updatedAt, lastOnline, email (tag), enable, group, traffic, expiryTime. Rows are slim — fetch /get/:email for the full  */
  listPaged<T = unknown>(params: { page: number; pageSize: number; search: string; filter: string; sort: string; order: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/clients/list/paged', params, options);
  },

  /** One client with everything the edit and info dialogs need: key, group, expiry, comment, plus the devices and IP log collected from the nodes. */
  getByEmail<T = unknown>(email: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/clients/get/${enc(email)}`, params, options);
  },

  /** Create a client. The key (uuid) is generated when omitted. This writes to the draft — the client cannot connect anywhere until the draft is published to the nodes. */
  add<T = unknown>(body: { client: Record<string, unknown>; inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/add', body, { ...JSON_HEADERS, ...options });
  },

  /** Replace a client record: tag, key, expiry, comment, group. The server replaces the row rather than patching it, so send the full set you want to keep. A draft edit — nodes keep serving the old record until the next publish. Changing the key */
  updateByEmail<T = unknown>(email: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/update/${enc(email)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Remove a client from the draft. Its traffic history, devices and IP log are dropped with it unless keepTraffic=1 — those are collected records, not configuration, and outlive the client only if asked. The client keeps working on the nodes u */
  delByEmail<T = unknown>(email: string | number, body: unknown, params: { keepTraffic: number }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/del/${enc(email)}`, body, { params: params, ...options });
  },

  /** Attach a client to entrypoints directly, bypassing its group. The normal path is to put the entrypoint in a group and give the client that group; this exists for the one-off case. Draft edit. */
  attachByEmail<T = unknown>(email: string | number, body: { inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/${enc(email)}/attach`, body, { ...JSON_HEADERS, ...options });
  },

  /** Remove entrypoints attached directly to this client. Entrypoints the client reaches through its group are unaffected — drop the group instead. Draft edit. */
  detachByEmail<T = unknown>(email: string | number, body: { inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/${enc(email)}/detach`, body, { ...JSON_HEADERS, ...options });
  },

  /** Zero the accumulated traffic of every client. Panel-side bookkeeping only: the nodes keep counting and are told nothing, the panel simply moves its baseline. Nothing is published and no client is interrupted. */
  resetAllTraffics<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/resetAllTraffics', body, options);
  },

  /** Delete many clients in one draft edit, processed in order so each delete sees the previous one committed. keepTraffic=true retains their collected traffic, devices and IP log. */
  bulkDel<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkDel', body, { ...JSON_HEADERS, ...options });
  },

  /** Put many clients into a group at once, replacing whatever group they carried. A group name that does not exist yet is created. To take clients out of a group use /groups/bulkRemove. Draft edit. */
  groupsBulkAdd<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/bulkAdd', body, { ...JSON_HEADERS, ...options });
  },

  /** Take many clients out of their group. The clients stay; only the group is cleared, which leaves them with whatever entrypoints are attached to them directly — possibly none. Draft edit. */
  groupsBulkRemove<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/bulkRemove', body, { ...JSON_HEADERS, ...options });
  },

  /** Attach many clients to many entrypoints in one call. Clients already attached to a target are reported as skipped rather than failing the batch. Draft edit. */
  bulkAttach<T = unknown>(body: { emails: unknown[]; inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkAttach', body, { ...JSON_HEADERS, ...options });
  },

  /** Detach many clients from many entrypoints in one call. Only direct attachments are touched; pairs that were never attached are silently skipped. Reaching an entrypoint through a group is unaffected. Draft edit. */
  bulkDetach<T = unknown>(body: { emails: unknown[]; inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkDetach', body, { ...JSON_HEADERS, ...options });
  },

  /** Every group with its entrypoint set and member count. A group holds entrypoints, not clients: assigning it to a client is what grants that client those entrypoints and what fills its connection URI. */
  groups<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/clients/groups', params, options);
  },

  /** Create an empty group. It becomes selectable immediately, but a group without entrypoints grants nothing — add entrypoints with /groups/entrypoints. */
  groupsCreate<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/create', body, { ...JSON_HEADERS, ...options });
  },

  /** Rename a group and carry the new name to every client holding it, in one transaction. Returns how many clients were relabelled. Draft edit — the connection URIs handed out afterwards carry the new name. */
  groupsRename<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/rename', body, { ...JSON_HEADERS, ...options });
  },

  /** Replace the entrypoint set of a group. These become the only entrypoints its clients can reach and the only ones encoded into their connection URI. An empty set leaves the group granting nothing. Draft edit. */
  groupsEntrypoints<T = unknown>(body: { name: string; entrypointIds: number[]; deviceLimit: number; allowExit: boolean }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/entrypoints', body, { ...JSON_HEADERS, ...options });
  },

  /** Remove a group and clear it from every client that carried it. The clients themselves are kept — filter by group and use /bulkDel to remove them. Returns how many were cleared. */
  groupsDelete<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/delete', body, { ...JSON_HEADERS, ...options });
  },

  /** Zero one client's accumulated traffic. Same panel-side baseline shift as resetAllTraffics — the node's own counters are untouched. */
  resetTrafficByEmail<T = unknown>(email: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/resetTraffic/${enc(email)}`, body, options);
  },

  /** Tags of the clients currently connected, deduplicated across every node the panel can reach. Live state read from the nodes, not draft configuration. */
  onlines<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/onlines', body, options);
  },

  /** The same online tags, grouped by the node that reported them, so a client connected through two nodes is visible on both. Nodes the panel has no channel to are absent rather than empty. */
  onlinesByNode<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/onlinesByNode', body, options);
  },

  /** Entrypoint tags that carried traffic within the heartbeat window, grouped by node. Pairs with onlinesByNode so a client holding several entrypoints is only marked active on the ones it actually used. */
  activeInbounds<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/activeInbounds', body, options);
  },

  /** Client tag to the unix timestamp of its last connection. This is the last time the client established a tunnel, not the last time it fetched anything from the panel. */
  lastOnline<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/lastOnline', body, options);
  },
};

/** Nodes */
export const nodesApi = {
  /** Every node with its role, health and revision. appliedRevision is what the node is actually running — compare it against the last published revision to see which nodes still owe an apply. */
  list<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/nodes/list', params, options);
  },

  /** Record a new node and mint the admin key only this node will accept. Nothing is contacted — the machine does not exist yet. The Add dialog turns the same values into a deploy script; running it on the new machine is what brings the node up, */
  add<T = unknown>(body: { name: string; role: string; address: string; port: number; apiToken: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/nodes/add', body, { ...JSON_HEADERS, ...options });
  },

  /** Change what the panel may change on a live node: tag and role. Address, port and admin key are baked in by the deploy script and are not editable here — changing them means redeploying. Switching the role changes which projection the node r */
  updateById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/update/${enc(id)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Take a node in or out of the network. A disabled node is skipped by publishes and stops being counted as a route for the groups holding its entrypoints; the machine keeps running until it is given a configuration that says otherwise. */
  setEnableById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/setEnable/${enc(id)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Forget a node. Its entrypoints go with it and drop out of every group that held them — clients whose group is left empty lose their route, so check the groups first. The machine is not touched and keeps running the last configuration it rec */
  delById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/del/${enc(id)}`, body, options);
  },

  /** Open the control channel now and refresh the node's cached health — latency, load, uptime, applied revision. Drives both the manual probe and the Reconnect button on an offline node. Latency is measured panel to node and describes the opera */
  probeById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/probe/${enc(id)}`, body, options);
  },

  /** Signal every reachable node to restart its datapath. Offline nodes are reported as skipped rather than queued. The node must flush its accumulated counters to disk before restarting — XDP maps reset to zero, and a restart that skips the flu */
  restartAll<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/nodes/restartAll', body, options);
  },

  /** Aggregated metric history recorded by the node's own runtime worker. The worker samples continuously and persists locally, so a window the panel was closed for is still returned once it reconnects. */
  historyByIdByMetricByBucket<T = unknown>(id: string | number, metric: string | number, bucket: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/nodes/${enc(id)}/history/${enc(metric)}/${enc(bucket)}`, params, options);
  },

  /** Raw, unaggregated samples for the requested window, as stored by the node worker. Intended for saving a local copy, not for charting. */
  historyExportById<T = unknown>(id: string | number, params: { bucket: number }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/nodes/${enc(id)}/history/export`, params, options);
  },

  /** Tail the node's log over the control channel, so the node has to be reachable — an offline node returns nothing rather than a cached copy. */
  logsByIdByCount<T = unknown>(id: string | number, count: string | number, body: { level: string; syslog: boolean }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/${enc(id)}/logs/${enc(count)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Whole log bundle from the node as one blob — every level, not the filtered view. For saving a local copy. */
  logsDownloadById<T = unknown>(id: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/nodes/${enc(id)}/logs/download`, params, options);
  },
};

/** Publish */
export const publishApi = {
  /** Summary of what the local draft changes against the last published revision. Drives the Publish/Discard pair in the header: absent or empty means the draft matches what the nodes run, and neither button is shown. */
  draft<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/publish/draft', params, options);
  },

  /** Throw the draft away and return to the last published revision. Local only — nothing is sent to any node. Without this a draft the operator decided against would still be waiting at the next panel start. */
  discard<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/publish/discard', body, options);
  },

  /** Phase 1. Freeze the draft into a new revision and build each node's configuration from it. Returns the participating nodes — the ones the panel currently holds a control channel to. Nodes already known to be unreachable are listed as skippe */
  plan<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/publish/plan', body, options);
  },

  /** Phase 2. Ship the planned configuration to every target. A node that fails is retried three times, three seconds apart, before it is reported as failed; the operator can then retry it by hand. Delivered configuration is staged on the node a */
  push<T = unknown>(body: { revision: number; nodeIds?: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/publish/push', body, { ...JSON_HEADERS, ...options });
  },

  /** Phase 3. Tell every node holding the staged revision to switch to it. The switch is seamless — established sessions are not dropped. A node applies only the revision it actually staged, so an apply can never activate a partial push. */
  apply<T = unknown>(body: { revision: number }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/publish/apply', body, { ...JSON_HEADERS, ...options });
  },

  /** Current state of the rollout in flight, per node. Lets the publish dialog be closed and reopened without losing the run — reopening resumes at the phase the rollout is actually in rather than starting over. */
  status<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/publish/status', params, options);
  },
};

/** Backup */
export const backupApi = {
  /** Collect the live database from every reachable node, compare the copies by checksum, and return a single snapshot. Nodes that disagree are reported so a node that silently drifted is visible instead of being averaged away. */
  pull<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/backup/pull', body, options);
  },

  /** Push a snapshot taken earlier to every reachable node and activate it. Runs through the same stage-then-apply path as a publish, so a restore cannot leave half the network on the old database and half on the new one. */
  restore<T = unknown>(form: FormData, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/backup/restore', form, options);
  },
};

/** Settings */
export const settingsApi = {
  /** Every panel preference. Six fields — page size, expiry and traffic warning thresholds, remark model, datepicker, timezone. These change how the panel renders and when it warns; none of them reach a node, and none of them appear in any publi */
  panelSettingAll<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/all', body, options);
  },

  /** The values a fresh install starts from. Same shape as /all. */
  panelSettingDefaultSettings<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/defaultSettings', body, options);
  },

  /** Persist the panel preferences. The panel saves these on its own shortly after they settle rather than behind a button — the header is reserved for the network draft, which is the thing that has to be published deliberately. */
  panelSettingUpdate<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/update', body, { ...JSON_HEADERS, ...options });
  },

  /** Restart the panel process. Rarely needed: preferences take effect immediately and nothing here is bound to a listening port. The connection drops and the panel comes back on the same URL. */
  panelSettingRestartPanel<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/restartPanel', body, options);
  },
};

/** WebSocket */
export const websocketApi = {
  /** Upgrade to a WebSocket for live pushes. Carries what would otherwise be polled — machine status, notifications, and cache invalidations raised by the panel's own work. */
  ws<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/ws', params, options);
  },
};
